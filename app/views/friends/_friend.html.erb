<tr id="<%= dom_id friend %>">
    <tr>
      <th scope="row">
        <%= link_to friend.first_name + ' ' + friend.last_name, friend %> 
      </th>
      <th scope="row"><%= friend.email %></th>
      <th scope="row"><%= friend.phone %></th>
      <th scope="row"><%= friend.twitter %></th>
</tr>
